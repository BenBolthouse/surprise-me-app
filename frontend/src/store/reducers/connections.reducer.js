/** @ignore */
const model = {
  requested: [],
  pending: [],
  approved: [],
};

const reducer = (state = model, { type }) => {
  switch (type) {
    default:
      return state;
  }
};

export default reducer;
